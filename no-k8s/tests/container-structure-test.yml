schemaVersion: "2.0.0"

commandTests:
  - name: "kubectl version"
    command: "kubectl"
    args: ["version", "--client=true"]
    expectedOutput:
      - Client Version
      - GitVersion:"v1\.\d+\.\d+"
